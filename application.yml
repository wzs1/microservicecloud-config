spring:
  profiles:
    active: 
    - dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-wzs-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-wzs-test